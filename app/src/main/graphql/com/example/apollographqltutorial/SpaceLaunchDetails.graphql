query SpaceLaunchDetails($launchId: ID!) {
    launch(id: $launchId) {
        id
        mission_name
        launch_date_local
        details
        launch_success
        launch_site {
            site_name
        }
        rocket {
            rocket_name
        }
        links {
            flickr_images
        }
    }
}
